{
  "name": "@telegram-lite/desktop",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "echo run 'npx tauri dev' after installing Rust toolchain",
    "build": "echo desktop tauri build configured (manual: npx tauri build)",
    "lint": "echo \"lint desktop\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"tests desktop\"",
    "format": "echo \"format desktop\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "typescript": "^5.6.3"
  }
}

